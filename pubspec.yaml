name: manga_ps
description: "Manga PS App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^4.15.5
  firebase_core: ^2.27.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/bg.jpg
    - assets/logo.jpg

flutter_launcher_icons:
  android: true
  ios: false 
  image_path: "assets/logo.jpg"
  min_sdk_android: 21
